// concatenate : 문자열을 +로 연결할 수 있다

let firstName = "Alex";
let lastName = "Kim";

console.log("Hello, " + firstName + " " + lastName);

// Template literal(Python: F-string)
let message = `내 이름은 ${firstName} ${lastName}입니다.`;
console.log(message);
